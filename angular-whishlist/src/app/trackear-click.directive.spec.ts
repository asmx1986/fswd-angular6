import { TrackearClickDirective } from './trackear-click.directive';

describe('TrackearClickDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackearClickDirective();
    expect(directive).toBeTruthy();
  });
});
